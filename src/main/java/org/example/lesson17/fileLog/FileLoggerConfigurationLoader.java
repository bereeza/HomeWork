package org.example.lesson17.fileLog;

import org.example.lesson17.Log.LogConfiguration;
import org.example.lesson17.Log.LogConfigurationLoader;

public class FileLoggerConfigurationLoader extends LogConfigurationLoader {
    @Override
    public LogConfiguration load(LogConfiguration config) {
        return super.load(config);
    }
}